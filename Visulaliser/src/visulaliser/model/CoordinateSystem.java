package visulaliser.model;

public class CoordinateSystem {
	
	/**
	 * finds the node with maximum lat, long 
	 * finds the node with minimum lat, long
	 * adds a value to max(lat,long), subtracts the same value from min(lat,long)
	 * converts all the other (lat,long) coordinates in terms of the 'corners' of the square
	 */
	
	

}
